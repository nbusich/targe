# Inherit everything from sft_full, but override specific keys
defaults:
  - sft_full

num_train_epochs: 1
max_steps: 1
per_device_train_batch_size: 1
gradient_accumulation_steps: 1
warmup_steps: 0
logging_steps: 1
save_steps: 1
save_total_limit: 1
push_to_hub: false
dataloader_num_workers: 8